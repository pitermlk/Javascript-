const valores = [20, 30, 40, 50]

const soma =  e  =>  e + e
const divisao = e => e / 2

resultado = valores.map(soma)

console.log(resultado)

resultado = valores.map(divisao)

console.log(resultado)